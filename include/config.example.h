#pragma once

// Copy this file to config.h. Never commit the real credentials.
#define MAKERLOG_WIFI_SSID "your-wifi"
#define MAKERLOG_WIFI_PASSWORD "your-password"
#define MAKERLOG_HOSTNAME "makerlog"

// The bootstrap account is only used when /system/users.json does not exist.
// Change this before the first flash.
#define MAKERLOG_ADMIN_PASSWORD "change-me-now"

